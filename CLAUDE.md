# Claude entrypoint for ChopDot

Claude and Claude Code must follow [`AGENTS.md`](AGENTS.md). This file is only a
portable entrypoint and intentionally carries no independent product, stack,
authority, priority, test-count, or release claims.

Before material work, run:

```bash
npm run context:validate
npm run agent:validate
```

`PRODUCT_TRUTH.md` remains product law. The Product Cockpit remains current
product routing. The Portable Agent Outcome System governs bounded agent-run
contracts and evidence; it cannot create product or participant authority.
