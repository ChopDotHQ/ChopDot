# Normal mode rule

Normal user mode must not expose journey numbers or reviewer-only state identifiers. The reviewer panel is diagnostic tooling and is opened explicitly; it is not part of the ordinary ChopDot navigation model.
