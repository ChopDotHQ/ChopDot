import fs from 'node:fs';
import path from 'node:path';
import assert from 'node:assert/strict';
import {createHash} from 'node:crypto';
import {execFileSync} from 'node:child_process';
const root=path.resolve(import.meta.dirname,'..'),j='journeys/13-request-money';
const read=p=>fs.readFileSync(path.join(root,p),'utf8'),json=p=>JSON.parse(read(p));
const hash=bytes=>createHash('sha256').update(bytes).digest('hex');
const html=read(`${j}/v1-candidate.html`),sha=hash(html);
assert.equal(sha,'a22664499c4056d95a6cdeb45df85d43fd2055713e80ae8f69020469aa9bb707');
const qa=json(`${j}/QA_SUMMARY.json`),browser=json(`${j}/visual-qa/browser-qa.json`);
const styles=s=>[...s.matchAll(/<style\b[^>]*>([\s\S]*?)<\/style>/gi)].map(m=>m[1]);
const inherited=styles(read('journeys/09-manage-people/v1-candidate.html')),current=styles(html);
assert.equal(current[0],inherited[0]);assert.equal(current[1],inherited[1]);
assert.equal(hash(current[0]),qa.inherited_css_sha256);assert.equal(hash(current[1]),qa.inherited_people_css_sha256);
for(const source of ['people-fixtures.cjs','model.cjs','ui.js'])assert(html.includes(read(`${j}/source/${source}`)),`Inline/source drift: ${source}`);
assert.equal(read(`${j}/source/people-fixtures.cjs`),read('journeys/09-manage-people/source/model.cjs'));
const model=JSON.parse(execFileSync(process.execPath,[path.join(root,j,'source/test-model.cjs')],{encoding:'utf8'}));
assert(model.ok);assert.equal(model.assertions,112);assert.equal(model.scenarios,44);assert.equal(model.cases.length,44);assert(model.cases.every(c=>c.passed));
assert.deepEqual(model,json(`${j}/visual-qa/model-qa.json`),'Recorded model evidence differs from executed result');
assert(qa.ok&&browser.ok);assert.equal(qa.artifact_sha256,sha);assert.equal(browser.artifact_sha256,sha);
assert.equal(browser.scenarios,30);assert.equal(browser.product_clicks,80);assert.equal(browser.layout_checks,44);
assert.equal(browser.checks.length,30);assert(browser.checks.every(c=>c.passed));assert.deepEqual(browser.page_errors,[]);
assert.equal(browser.layouts.length,44);assert(browser.layouts.every(c=>c.passed&&c.nonblank&&!c.overflow&&!c.headerOverlap&&!c.footerOverlap&&c.footerVisible&&c.clipped===0));
const screens=json(`${j}/SCREEN_STATE_MAPPING.json`),actions=json(`${j}/UI_EVENT_MAPPING.json`);
assert.equal(screens.length,22);assert.equal(new Set(screens.map(s=>s.screen)).size,22);assert.equal(actions.length,72);
for(const s of screens)for(const viewport of ['393','430'])assert(browser.layouts.some(l=>l.route===s.screen&&l.viewport===viewport&&l.passed));
for(const action of actions){assert(action.domain_event&&action.guard);assert(screens.some(s=>s.screen===action.route));}
for(const doc of ['spec.md','STATE_AND_AUTHORITY.md','GIVEN_WHEN_THEN.md','UI_TO_DOMAIN_EVENTS.md','STATE_INVENTORY.md','VISUAL_QA.md'])assert(read(`${j}/${doc}`).trim().length>100);
const journeys=json('registry/journeys.json'),progress=json('registry/progress.json');
assert.equal(progress.golden_count,12);assert.equal(progress.remaining_overall,16);assert.equal(progress.current_journey,'13');
assert.equal(journeys.find(x=>x.id==='09').status,'golden');assert.equal(journeys.find(x=>x.id==='09').golden_number,12);
assert.equal(journeys.find(x=>x.id==='13').status,'current');assert.equal(journeys.find(x=>x.id==='13').approval,'review-pending');
for(const manifest of ['next-support-candidate','support-candidate','active-candidate']){const a=json(`registry/${manifest}.json`);assert.equal(a.journey,'13');assert.equal(a.prototype_sha256,sha);}
const locks=json('registry/golden-artifact-locks.json');assert.equal(locks.length,12);assert(!locks.some(l=>l.journey==='13'));
for(const l of locks)assert.equal(hash(fs.readFileSync(path.join(root,l.path))),l.sha256,`Golden changed: ${l.path}`);
const approval=json('registry/approvals/09-v1.json');assert.equal(approval.deferred_note.change_now,false);
assert.equal(approval.approved_policy.removal_authority,'current-group-owner-only');assert.equal(approval.approved_policy.removal_condition,'no-open-items-for-this-person-in-this-group');
assert(approval.approved_policy.past_records_preserved&&approval.approved_policy.other_memberships_preserved);
const validation={ok:true,journey:'13',version:'v1',candidate_sha256:sha,states:22,model_assertions:112,model_scenarios:44,browser_scenarios:30,product_clicks:80,state_layout_checks:44,mapped_actions:72,inherited_css_unchanged:true,inherited_people_css_unchanged:true,all_golden_locks_pass:true,golden_count:12,current_journey:'13',review_status:'review-pending',typography_note:'TYPO-01 deferred',ci_executes:'Model tests and verification of checksum-bound recorded browser evidence; browser suite is not rerun by this gate.',limitations:'Synthetic in-memory prototype. Playwright inline installed Chromium; no real backend, notifications, payment, hosted/native-file delivery or full cross-journey integration.'};
fs.writeFileSync(path.join(root,j,'validation.json'),JSON.stringify(validation,null,2)+'\n');
console.log('J13 REQUEST GATE PASSED: 112 model assertions, 30 artifact-bound browser scenarios, 44 layouts; all 12 Goldens unchanged.');
