import fs from 'node:fs';import path from 'node:path';import {createHash} from 'node:crypto';
const root=path.resolve(import.meta.dirname,'..');const read=f=>fs.readFileSync(path.join(root,'source',f),'utf8');
const results='<div class="labsec"><div class="lablabel">Simulated service results</div><button class="compare" data-result="accepted">Save accepted</button><button class="compare" data-result="not-saved">Verified not saved</button><button class="compare" data-result="delivered">Delivery confirmed</button><button class="compare" data-result="delivery-failed">Delivery failed</button><button class="compare" data-result="reconnect">Reconnect · review again</button></div>';
const html=`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><title>ChopDot — Request Money V1</title><style>${read('inherited.css')}</style><style>${read('people.css')}</style><style>${read('request.css')}</style></head><body><div class="lab"><div class="stage"><div class="device" id="device"></div></div><aside class="labpanel mp-lab"><h2>Journey 13 · Request Money</h2><p>Review candidate · simulated data only.</p><div class="labsec"><div class="lablabel">Current context</div><p id="lab-state"></p></div><div class="labsec"><div class="lablabel">Click-through scenarios</div><div data-demo-list></div></div>${results}<div class="labsec"><p>No real request, notification or payment is sent. Creation, delivery and payment are separate. All 12 Goldens remain unchanged. TYPO-01 stays deferred.</p></div></aside></div><dialog class="mp-dialog mp-lab" id="demo-dialog"><button class="close-demo" onclick="this.closest('dialog').close()">Close demo controls</button><p>Prototype only · no real requests or payments</p><div data-demo-list></div>${results}</dialog><script>${read('people-fixtures.cjs')}</script><script>${read('model.cjs')}</script><script>${read('ui.js')}</script></body></html>\n`;
fs.writeFileSync(path.join(root,'v1-candidate.html'),html);console.log(createHash('sha256').update(html).digest('hex'));
