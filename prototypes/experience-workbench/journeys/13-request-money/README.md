# Journey 13 — Request Money V1

Open `v1-candidate.html`. This is Candidate #13, not approved or frozen.

Start with Marc's CHF 30.00 in Zurich Weekend: optional note → Review request → Send request. Use Demo for all-groups, separate CHF/DOT, duplicate, unknown result, offline, changed balance and withdrawal examples. Service results are simulations, not real requests or payments.

Build: `node source/build.mjs`. Model tests: `node source/test-model.cjs`. Browser tests: `python source/browser-tests.py` with Playwright and installed Chromium. The browser script is configured for the reviewed inline-artifact environment; hosted/native-file behavior is not claimed.

Read `spec.md`, `STATE_AND_AUTHORITY.md`, `GIVEN_WHEN_THEN.md`, mappings and `VISUAL_QA.md`. Full reload resets the in-memory demo. Keep shared typography/readability deferred as TYPO-01. Journey 09 remains Golden #12 with its reviewed HTML unchanged.
