# This portable shell is a static browser app.
# Do not put private API keys, bot tokens, wallet secrets, or backend credentials here.

# Optional public app URL used by deployment tooling outside the browser bundle.
APP_URL="https://example.com"
