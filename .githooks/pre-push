#!/bin/sh
set -eu

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

node scripts/agent-governance/steering-surfaces.mjs check --require-promoted
node scripts/agent-governance/adoption-guard.mjs push
